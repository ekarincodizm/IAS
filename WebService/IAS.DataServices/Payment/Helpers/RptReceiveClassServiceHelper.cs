using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using IAS.DataServices.Class;
using System.Configuration;

namespace IAS.DataServices.Payment.Helpers
{
    public static class RptReceiveClassServiceHelper
    {
        //public static String GetFullPath(this RptReciveClassService receipt) {
        //    string PDF_Receipt = ConfigurationManager.AppSettings["FS_RECEIVE"].ToString(); // "ReceiptFile";
        //    string FileNameInput = receipt.i + "_" + RecNo + ".pdf";
        //    string path = PDF_Receipt + @"\" + idC;

        //    string mapDrive = ConfigurationManager.AppSettings["DEFAULT_NET_DRIVE"].ToString();
        //    String dirpath = Path.Combine(mapDrive, PDF_Receipt, idC);
        //    DirectoryInfo dir = new DirectoryInfo(dirpath);
        //    if (!dir.Exists)
        //        dir.Create();
        //} 
    }
}